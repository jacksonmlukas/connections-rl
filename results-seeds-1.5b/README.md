# `results-seeds-1.5b/` — 1.5B three-seed endpoint session

Held-out eval of the three 1.5B GRPO seeds (`grpo-seed0/`, `grpo-seed1/`,
`grpo-seed2/`) against a shared `sft/` baseline, one serving session. The 1.5B
scale contrast: at this scale there was no grouping ability to trade away, so
the same training reads as a pure format-validity gain.

**Cited by:** the repo README's seed-replication table (1.5B rows). Companion
sampling-budget artifact: `results-analysis/passk-1.5b.json`.

**These files are an immutable record.** Regenerate elsewhere; never edit in place.
